# Use a dedicated test database: tests delete all data before every test.
DATABASE_URL="postgresql://postgres:password@localhost:5432/project_backend_test"
SESSION_SECRET="replace-with-a-test-only-secret"
